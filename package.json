{
  "name": "kinetic-push-api",
  "private": true,
  "description": "Vercel serverless functions for Kinetic rest-timer Web Push (VAPID + QStash delay). The static PWA in /app has NO build step; this package.json only exists so Vercel installs web-push for the /api functions.",
  "dependencies": {
    "web-push": "^3.6.7"
  }
}

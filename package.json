{
  "name": "kinetic-pwa-tracker",
  "private": true,
  "description": "Kinetic — static vanilla-JS PWA in /app (no build step). Root deps: web-push for the Vercel /api push functions. devDeps: Capacitor 8 + capacitor-health for the optional native iOS wrapper (see NATIVE-HEALTHKIT-SETUP.md). NOT needed by the web deploy.",
  "dependencies": {
    "web-push": "^3.6.7"
  },
  "devDependencies": {
    "@capacitor/cli": "^8.0.0",
    "@capacitor/core": "^8.0.0",
    "@capacitor/ios": "^8.0.0",
    "capacitor-health": "^8.0.0"
  }
}
